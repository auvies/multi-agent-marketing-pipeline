services:
  - type: web
    name: campaign-backend
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: ANTHROPIC_API_KEY
        scope: build
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001
